# ONE GHOST MODE

初期MVPのシンプルな面白さを残すための、**独立した検証モード**。

タイトル画面で `STANDARD MVP` / `ONE GHOST MODE` を選ぶ。
バランス定数は [`src/config.ts`](src/config.ts) の `CONFIG.oneGhost` に分離してあり、
**ここを触っても STANDARD MVP の挙動は一切変わらない**（逆も同じ）。

---

## 1. 何を検証するモードか

通常モードは「廃墟そのものとViewer Requestが怖い」。
ONE GHOST MODE は「**一体の怪異との距離感そのものが怖い**」。

検証したいのはコンテンツ量ではなく、次の感想が出るかどうか。

- 一体しかいないのにずっと気になった
- 離れた方がいいのに近づいてしまった
- もうHEYしない方がいいと思ったけど押した
- 逃げ切ったのにまた戻ってしまった
- 追われてるのに振り返って撮ってしまった

特に **「逃げた後に自分から戻る」** が起きれば成功。

---

## 2. 通常モードとの差分

| | STANDARD MVP | ONE GHOST MODE |
| --- | --- | --- |
| 怪異 | 一体 + 環境怪異7種 | **一体だけ** |
| 調査地点 | 6か所（見る→異変→触る→自撮り） | **無し**（小物は背景として残る） |
| `[E]` | INSPECT / ANSWER / LEAVE | **LEAVEだけ** |
| Haunted Level | 異変の解禁・コメント加速に使用 | 怪異の活発さにだけ関与 |
| リクエスト | 30種以上・目的地系あり | **5〜6種・移動クエスト無し** |
| 帰り際の誘惑 | 7種（GO BACK / CARRY THE DOLL など） | **ONE LAST CALL だけ** |
| 追跡 | 捕まれば終わり | **5〜15秒の撤退戦。逃げ切れば続く** |
| 静かな時間の埋め方 | 環境怪異を発生させる | **怪異自身を動かす** |
| ステージ | 10部屋の回遊型（廊下・ドア・小部屋） | **一部屋だけ**（30m × 34m + 柱8本） |

---

## 2.5 ステージは一部屋だけ

```text
   ┌───────────────────────────────┐  z = 1
   │  ▮        ▮        ▮        ▮ │   ← 崩れた柱。部屋は分けない
   │            怪異 (0, 5)         │
   │  ▮        ▮        ▮        ▮ │
   │                               │
   │  ▮        ▮        ▮        ▮ │
   │        ○ (0,31) [EXIT] (0,33) │   ← 入口＝出口＝開始地点
   └───────────────────────────────┘  z = 35
     x = -15                  x = 15
```

[`src/world/ghostLevel.ts`](src/world/ghostLevel.ts)。廊下も別室も無く、
**探索できる場所が一つも無い**ので、プレイヤーがやることは
「一体との距離をどうするか」だけになる。

柱は部屋を分割しない。中央の `x -3..3` は空けてあるので、
入口に立った時点で部屋の奥の怪異が見通せる（§4：長い探索をさせない）。
回り込めば一瞬だけ姿が切れるので、Peeking と Stalking は成立する。

入口の座標（`0, 33`）は通常モードと共有しているため、
帰宅判定・`[E]` の距離・プレイヤーの開始位置はそのまま使い回している。

---

## 3. 基本ループ

```text
怪異を見つける → 安全距離から撮る → もっと近づくと報酬が増える
  → 怪異も反応する → もう少し近づく？ → HEYする？ → さらに撮る？
  → 逃げる？ → また戻る？
```

### 開幕

部屋の最奥にすでに立っている（`oneGhost.monsterSpawn` = `0, 5`。入口から26m）。
探させない。ただし**姿が見えているだけでは配信は反応しない**。

`oneGhost.discoverDistance`（20m）まで**自分から近づいて初めて**発見になり、
その瞬間に Viewer 増加・Like・コメント・`+¥800`（`discoveryBonus`）が入る。

> 見えている → でも何も起きない → 一歩踏み込む → 跳ねる

という順番を作るための距離。

### 撮るだけでも稼げる

リクエストが無くても、怪異をうまく撮れば Like / Viewer / 収益が伸びる。
被写体が一体しかないため `oneGhost.monsterClipMult`（×1.35）で撮影価値を底上げしている。

画面には Clip Value やDangerの数値は出さない。Like・収益・Viewerの変化だけで伝える。

---

## 4. 怪異の段階（§11）

内部の `MonsterState` と、このモードでの呼び方の対応。デバッグパネル `[P]` はモード名で表示する。

| 表示 | 内部 | Danger | 何が起きるか |
| --- | --- | --- | --- |
| DORMANT | `dormant` | 0 | 遠くに立っているだけ。ほぼ動かない |
| OBSERVED | `observed` | 15 | 少し頭を動かす / こちらを見る。まだかなり安全 |
| AWARE | `aware` | 32 | こちらを向く / 数歩近づく / 視界外で位置を変える |
| **STALKING** | `aggressive` | **46** | **こちらが見ている間は止まり、見ていない間に詰めてくる** |
| AGGRESSIVE | `hunting` | 72 | 突進フェイント / 数秒の急接近（捕まっても死なない） |
| CHASING | `chasing` | 90 | 本追跡。捕まれば死ぬ |

**STALKINGの帯を一番広く取ってある**（46〜72）。Chaseより
「あいつさっきより近くない？」の時間を長くするのがこのモードの中心。

- 見られている間は止まる: `oneGhost.stalkFreezeCenter`
- 見ていない間に詰める距離: `oneGhost.stalkDistanceHidden`（6.5m）
- 通常モードと違い、**近づいても逃げない**（近づけないとゲームが成立しないため）

### Dangerの上がり方

このモードは距離そのものがゲームなので、接近を強く効かせている。

```text
oneGhost.proximity      = { proximityPerSec: 16, proximityRange: 9 }
oneGhost.dangerDecayPerSec = 1.2   // 常時、無条件に引く
```

通常モードは「上昇が0の間だけ減る」が、このモードでは怪異を撮り続けるのが常態なので、
それだと**撮っているだけでDangerが100まで登ってしまう**。常に引くことで、

- **約7.5mより外 → いくら撮っても安全**（Dangerは0へ戻る）
- 6m → 毎秒+0.9 / 5m → +2.3 / 3m → +6.2

という「近づいた分だけ関係が悪くなる」曲線になる。

---

## 5. HEY（§21〜§26）

怪異が一体しかいないため、HEYが**関係を変える基本操作**になる。
3回目までは押した回数がそのまま段階になり、4回目以降だけ幅を持たせる。

| 回数 | 反応 |
| --- | --- |
| 1 | こちらを見る。撮れ高増加、危険はまだ低い |
| 2 | 少し近づく。Viewer大幅増加 |
| 3 | STALKINGへ（至近距離なら突進フェイント） |
| 4+ | 無反応 / 遅れて反応 / 横へ移動 / 急接近 / フェイント / Chase から状態に応じて選択 |

姿が見えないときも使える。足音・声・一瞬の姿で**位置が分かる**（情報としての価値）。
ただし自分の位置も知らせる。

Dangerは通常モードより緩い（`oneGhost.hey.streakDanger` = `[4, 8, 13, 20]`、
距離倍率は最大×1.5）。**4回押しただけでCHASINGまで行くと「自分で決めた」感覚が消える**ため、
4回でおよそ STALKING〜AGGRESSIVE 止まりになる値にしてある。

---

## 6. Viewer Request（§8〜§10）

Acceptボタンは無い。**やれば達成、やらなければ時間切れ**。
「○○へ行け」「○○を探せ」という移動クエストは一切出さない。

### メインのチキンレース（`oneGhost.request.chainRewards`）

```text
+¥500     GET CLOSER        8m以内に入る
+¥1,500   GET EVEN CLOSER   5m以内に入る
+¥3,000   CALL OUT TO IT    [Q]
+¥6,000   CALL IT AGAIN     [Q] 連続で
+¥10,000  KEEP FILMING      6秒間フレームに入れ続ける
+¥15,000  TAKE A SELFIE WITH IT
```

- 達成した瞬間に**即時確定**。仮獲得の会計は入れていない
- カードには常に `NEXT +¥X — TITLE` が出る（降りるか進むかの材料）
- 15%の確率でそこで打ち切られる（毎回最後まで出ない）
- すでに近い位置にいる場合、`GET CLOSER` の段は飛ばして始まる（無料報酬を避ける）

### Selfie Chicken Race（§28）

Selfie中に怪異が背後に入っていると別のラダーが始まる。

```text
+¥5,000   CALL IT WHILE TAKING A SELFIE
+¥10,000  CALL IT AGAIN
+¥15,000  DON'T TURN AROUND  （Selfieのまま5秒。解除した瞬間に失敗）
```

Selfieはいつでも解除して逃げられる。

### 追跡中（§18）

追跡が始まると `+¥8,000 FILM IT WHILE YOU RUN` が出る。

```text
前を向いて走る = 安全
振り返って撮る = 儲かるが危険
```

さらに撮る・自撮り・HEYをするたびに怪異が加速する
（`monster.chaseUrgency` 撮影+0.35 / 自撮り+0.7 / HEY+1.6、上限+1.8 m/s）。

### 単発

`DON'T MOVE`（8秒静止、¥3,000）を22%の確率で挟む。毎回ラダーだと単調になるため。

---

## 7. 追跡は撤退戦（§17〜§20）

**Chase = 即死ではない。Chaseしてもゲームは終わらない。**

```text
oneGhost.chase = {
  windup: 1.5,           // 走り出す前の溜め（この間に逃げる判断ができる）
  speed: 5.8,            // 追跡速度。走る(7.0)との差は 1.2 m/s
  minDuration: 10,       // これ未満では諦めない
  maxDuration: 15,       // 15秒経ったら諦める（距離では決着しないため）
  escapeDistance: 15,    // この距離を1.6秒保てば逃げ切り
  entranceSafe: 3.5,     // 扉の前まで戻れば逃げ切り
  entranceSafeAfter: 5,  // ただし5秒経つまでは扉に着いても解除しない
  dangerAfter: 44,       // 逃げ切ったあとは AWARE へ戻る
  lockout: 14,           // 逃げ切った直後は再追跡しない
}
```

通常モードは追跡開始時に怪異をアンカーへ瞬間移動させて距離を作るが、
このモードでは**目の前で瞬間移動させない**（§31に反する）。代わりに溜めで時間を渡す。

`lockout` は「帰るか、もう一度撮りに行くか」を落ち着いて決める時間を必ず作るためのもの。

### 一部屋だと距離では決着しない

部屋は34mしかないので、まっすぐ走ると3秒で壁に着く。
一方、部屋を大きく周回して逃げると距離は**約4〜5mで平衡してしまい**、
`escapeDistance` には永遠に届かない（追跡速度が走る速度の0.83倍のため）。

そこで **時間で必ず決着させる**。逃げ道は二択になる。

| 逃げ方 | かかる時間 | その間の撮れ高 |
| --- | --- | --- |
| **扉まで戻る** | 5秒（`entranceSafeAfter`） | 短い。走るだけ |
| **部屋を周回して粘る** | 15秒（`maxDuration`） | 長い。ただし撮ると死ぬ |

さらに、追われている最中でも扉で `[E]` を押せば配信を畳んで全額持ち帰れる。
**安全策は「逃げ切る」ではなく「ここで降りる」** でもある。

> 扉はすぐそこ。今降りれば全部持って帰れる。でも降りたら今日はもう終わり。

### 実測（ヘッドレス検証・10m から追跡開始）

| 逃げ方 | 前を向いて走る | 振り返って撮る |
| --- | --- | --- |
| 扉まで戻る | **逃げ切り 5.0秒**（最接近8.6m） | 逃げ切り 5.0秒（最接近8.0m） |
| 部屋を周回して粘る | **逃げ切り 15.0秒**（最接近4.7m） | **死亡 5.9秒** |
| 同・6mから | 逃げ切り 15.0秒（最接近5.4m） | **死亡 4.7秒** |

粘るほど撮れ高（追跡中は Clip Value ×4〜6）は伸びるが、
撮った瞬間に怪異が加速して詰められる。

---

## 8. ONE LAST CALL（§20）

追跡を逃げ切っても怪異はまだ居る。入口へ帰って `[E]` を押せば終われる。
そのタイミングで **移動クエストは出さない**。代わりに入口から撃てる誘惑を出す。

```text
+¥12,000  ONE LAST CALL     建物の奥へ向かって叫ぶ [Q]
+¥20,000  CALL IT AGAIN     何かが応えた。もう一度 [Q]
```

戻る必要はない。**また撮りに戻るかどうかはプレイヤーが決める。**

---

## 9. 静かな時間の埋め方（§32）

電話・仏壇・テレビ・死体袋は出さない。環境怪異の自動発生も止めている
（`AnomalySystem.autoSpawn = false`）。

22秒以上なにも起きないと、ディレクターが**怪異自身**を動かす。

1. 25%: 近くの蛍光灯を明滅させる
2. 見えている → こちらへ一歩踏み出す
3. 26m以上離れている → 近くへ回り込み、方向をヒントで知らせる
4. 近いのに見えていない → 物陰から半身を出す（Peeking）

完全ランダムなテレポートはしない。「あいつどこへ行った？」と思える範囲で動く。

---

## 10. ログとKPI（§38 / §39）

すべての行に `mode: "one_ghost"` が入る。ダウンロードファイル名も
`haunted-streamer-one_ghost-log-*.json` になる。

追加イベント:

```text
monster_state_changed / player_approached / player_retreat_started
selfie_started / selfie_ended / chase_started / chase_escaped
player_returned_after_escape / last_temptation_shown / last_temptation_taken
player_exited / player_died
```

リザルト画面の `ONE GHOST` パネルに出る指標:

| KPI | 意味 | 良い値 |
| --- | --- | --- |
| **Closest distance** | どこまで自発的に近づいたか | 6m以下 |
| **HEY count** | 何回自分から刺激したか | — |
| **Second HEY rate** | 一度HEYした後、二度目を押した割合 | 30%以上 |
| **Retreat → Return rate** | 一度離れた後、また撮りに戻った割合 | **1回でも起きれば成功** |
| **Chase greed rate** | 追跡中に振り返って撮った割合 | 50%以上 |
| **Escaped a chase, then went back** | 逃げ切った後に自分から戻った回数 | **最重要** |
| **Exit opportunities ignored** | 帰れる状態になったのに中へ戻った割合 | — |

---

## 11. バランス検証ボット

通常モードの `autoplay.ts` とは別。調査地点も異変も見ず、
怪異との距離とリクエストだけで行動を決める。

```js
const bot = await import('/src/dev/oneGhostBot.ts');
console.table(bot.runAllOneGhost());
```

`timid`（15mから撮るだけ・HEYしない）/ `curious`（報酬が見合うものには乗る）/ `greedy`（全部やる）。

### 実測（各4回）

| スタイル | 結果 | 時間 | 最接近 | HEY | 達成/提示 | 最長連鎖 | 追跡/逃げ切り | 収益 | 最大Danger |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| timid | 生還 4/4 | 2.5分 | 12.6〜13.6m | 0 | 0〜1/8 | 1 | 0 / 0 | ¥1,600〜5,200 | **0〜5** |
| curious | 生還 1/4 | 2.2〜4.1分 | 1.5〜4.7m | 0〜6 | 5〜11/16 | **2〜5段** | 0〜1 | ¥12,600〜78,400 | 56〜100 |
| greedy | 死亡 4/4 | 0.6〜1.3分 | 1.5m | 7〜15 | 2〜6/7 | 1〜4段 | 1〜4 / ほぼ逃げ切り | ¥15,600〜68,900 | 100 |

読み方:

- **遠くから撮っているだけなら Danger は 0〜5**。§13「まだかなり安全」を満たしている
- 近づいてHEYを押した分だけ Danger 100 に近づき、**逃げ切りは何度でも起きる**
- 死ぬのは「追われながら撮り続けた」場合だけ（ボットは追跡中も撮り続ける設定なのでよく死ぬ）
- 静かな時間の最長は 22秒（=`tempo.quietLimit`）。目標の30秒以内

### 人のプレイでしか測れないもの

ボットは即断即決するため、次の2つは0になる。

- **Avg hesitation**（金額を見て何秒迷ったか）
- **Retreat → Return**（怖くなって離れたのに、また戻ってしまう）

この2つが出るかどうかが、このモードの成否そのものになる。
